#include<iostream>
using namespace std;

int countDigits(int n,int sum) {
    // Base case: if n is 0, no digits remain
    // -- write here --
     ;
    if (n == 0)
    {
        return sum;
    }
    else {
        sum++;
        return countDigits(n/10,sum);
    }
    // Recursive case: strip one digit and count the rest
    // -- write here --
}

int main() {
    int sum = 0;
    cout << countDigits(12345,sum) << endl;  // Expected: 5
    cout << countDigits(9,sum) << endl;  // Expected: 1
    return 0;
}
